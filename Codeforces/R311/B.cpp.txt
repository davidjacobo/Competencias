#include iostream
#include cstdio
#include vector
#include algorithm
#define MAX_N 200001

using namespace std;
typedef long long ll;
ll pots[MAX_N], n;

int main(){
	double w;
	cinnw;
	for(int i=0;i(n1);++i){
		cinpots[i];
	}

	sort(pots, pots+(n1));

	double sum = 0;
	sum = pots[n]; min of the max vases
	
	double vol = pots[n]2.0;
	if(vol = pots[0]){
		n = pots[n]n;
		sum = n + n2.0;
	} else if((pots[0]1) = pots[n]){
		n = (pots[0]n);
		n+= (n1);
		sum = n;
	} 

	printf(%.10lfn, min(w, sum));	

	return 0;
}